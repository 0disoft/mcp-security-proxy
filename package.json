{
  "name": "mcp-security-proxy-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm typecheck && pnpm test && pnpm contract && pnpm run docs && pnpm run package-surface && pnpm run secret-scan && pnpm run compatibility && pnpm run license-report && pnpm run smoke",
    "compatibility": "pnpm build && node scripts/check-compatibility-evidence.mjs",
    "contract": "pnpm build && pnpm --filter @0disoft/mcp-security-proxy-contracts typecheck && pnpm --filter @0disoft/mcp-security-proxy-core test",
    "docs": "node scripts/check-docs.mjs",
    "license-report": "node scripts/check-dependency-licenses.mjs",
    "package-surface": "node scripts/check-package-surface.mjs",
    "secret-scan": "node scripts/check-secrets.mjs",
    "smoke": "pnpm build && node packages/cli/dist/main.js check-policy --policy fixtures/policies/local-dev.json --json && node packages/cli/dist/main.js eval-call --policy fixtures/policies/local-dev.json --profile local --input fixtures/mcp/call-file-read-denied.json --json && node scripts/smoke-live-run.mjs",
    "test": "pnpm build && pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
